import { Cell, Pie, PieChart, ResponsiveContainer } from 'recharts'
import { categorySpending, financialSummary } from '../../data/mocks'
import { formatCurrency } from '../../lib/formatters'

export function CategoryChart() {
  return <div><div className="grid grid-cols-[145px_1fr] items-center gap-2"><div className="relative h-44"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={categorySpending} dataKey="percentage" innerRadius={43} outerRadius={61} paddingAngle={0} stroke="none">{categorySpending.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 grid place-content-center text-center"><span className="text-[10px] text-muted">Total</span><strong className="tabular mt-1 text-xs">{formatCurrency(financialSummary.expenses).replace(',50', '')}</strong></div></div><ul className="space-y-2">{categorySpending.map((item) => <li key={item.name} className="grid grid-cols-[8px_1fr_auto] items-center gap-2 text-[11px]"><span className="size-2 rounded-full" style={{ background: item.color }} /><span>{item.name}</span><strong className="tabular">{item.percentage}%</strong></li>)}</ul></div><div className="mt-2 flex items-center justify-between rounded-control border border-border bg-surface-interactive p-3"><div><p className="text-[10px] font-semibold text-muted">MAIOR GASTO</p><p className="mt-1 text-xs font-medium">Alimentação</p></div><div className="text-right"><p className="tabular text-xs font-semibold">R$ 820,00</p><p className="mt-1 text-[10px] text-muted">24% do total</p></div></div></div>
}
